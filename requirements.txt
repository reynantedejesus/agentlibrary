# Runtime dependencies for the Agent Library.
# Pinned to compatible ranges that still support Python 3.9 (Rocky Linux 9's
# system Python). Install with:  pip install -r requirements.txt

Flask==3.0.3
Werkzeug==3.0.6
Jinja2==3.1.4
itsdangerous==2.2.0
click==8.1.7

Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36
Flask-Migrate==4.0.7
alembic==1.13.3

Flask-WTF==1.2.1
WTForms==3.1.2

Flask-Limiter==3.8.0

PyMySQL==1.1.1
cryptography==43.0.3          # required by PyMySQL for caching_sha2_password

python-dotenv==1.0.1          # development only; harmless in production
gunicorn==23.0.0
